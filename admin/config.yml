# ---------------------------------------------------------
# BACKEND : AUTHENTIFICATION PKCE VIA DECAPBRIDGE
# ---------------------------------------------------------
backend:
  name: github
  repo: geoffroydbs/aseedsorbonne
  branch: main
  auth_type: pkce
  base_url: https://auth.decapbridge.com
  # ID vérifié selon vos précédentes captures
  auth_endpoint: /sites/2fc63fc3-ce15-4508-adc1-62384b4d7296/pkce
  gateway_url: https://gateway.decapbridge.com

# ---------------------------------------------------------
# GESTION DES MÉDIAS (OBLIGATOIRE POUR ÉVITER LES ERREURS)
# ---------------------------------------------------------
media_folder: "public/images"
public_folder: "/images"

# ---------------------------------------------------------
# CONFIGURATION DE L'INTERFACE
# ---------------------------------------------------------
site_url: https://aseedsorbonne.com
display_url: https://aseedsorbonne.com
logo_url: https://decapbridge.com/decapcms-with-bridge.svg

# Personnalisation des messages de commit pour la traçabilité
commit_messages:
  create: "Create {{collection}} “{{slug}}” - {{author-name}} via DecapBridge"
  update: "Update {{collection}} “{{slug}}” - {{author-name}} via DecapBridge"
  delete: "Delete {{collection}} “{{slug}}” - {{author-name}} via DecapBridge"
  uploadMedia: "Upload “{{path}}” - {{author-name}} via DecapBridge"
  deleteMedia: "Delete “{{path}}” - {{author-name}} via DecapBridge"

# ---------------------------------------------------------
# COLLECTIONS DE CONTENU
# ---------------------------------------------------------
collections:
  - name: "settings"
    label: "⚙️ Configuration & Alerte"
    files:
      - name: "general"
        label: "Textes & Liens Principaux"
        file: "content/settings.json"
        fields:
          - {label: "Titre Principal", name: "headline", widget: "string"}
          - {label: "Message d'accueil", name: "subtext", widget: "text"}
          - {label: "Lien Annales (Bouton Noir)", name: "annales_url", widget: "string"}
          - {label: "Lien Instagram (Bouton Blanc)", name: "insta_url", widget: "string"}
      
      - name: "alert"
        label: "🚨 Badge de Statut (Pilule)"
        file: "content/alert.json"
        fields:
          - {label: "Afficher le badge ?", name: "show_alert", widget: "boolean", default: false}
          - {label: "Message court", name: "message", widget: "string", required: false}
          - label: "Couleur"
            name: "type"
            widget: "select"
            options: 
              - {label: "Orange", value: "orange"}
              - {label: "Vert", value: "green"}
              - {label: "Rouge", value: "red"}
              - {label: "Bleu", value: "blue"}
            default: "orange"
          - {label: "Lien au clic (Optionnel)", name: "link", widget: "string", required: false}

  - name: "content"
    label: "📅 Évènements & Actus"
    files:
      - name: "events"
        label: "Agenda"
        file: "content/data.json"
        fields:
          - label: "Liste des Évènements"
            name: "events"
            widget: "list"
            fields:
              - {label: "Mois (ex: JAN)", name: "month", widget: "string"}
              - {label: "Jour (ex: 25)", name: "day", widget: "string"}
              - {label: "Catégorie", name: "category", widget: "select", options: ["Vie Étudiante", "Culture", "Orientation", "Pédagogie", "Partenariat"]}
              - {label: "Titre", name: "title", widget: "string"}
              - {label: "Lieu", name: "location", widget: "string"}
              - {label: "Lien (Optionnel)", name: "url", widget: "string", required: false}
